{
  "name": "cookie-keeper",
  "module": "commonjs",
  "version": "1.0.2",
  "files": [
    "dist"
  ],
  "types": "dist/index.d.ts",
  "main": "dist/index.js",
  "scripts": {
    "prepare": "rm -rf dist && tsc --outDir dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "echolator": "^1.0.0"
  },
  "devDependencies": {
    "persistorm": "^2.0.1"
  }
}
